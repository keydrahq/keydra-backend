# Filled in by Maven resource filtering at build time (see pom.xml <resources>).
keydra.name=${project.name}
keydra.version=${project.version}
keydra.build.timestamp=${maven.build.timestamp}
keydra.build.commit=${git.commit.id.abbrev}
keydra.build.java-version=${maven.compiler.release}
keydra.build.quarkus-version=${quarkus.platform.version}
